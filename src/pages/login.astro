---
import Layout from "../layouts/Layout.astro";
import { Icon } from "astro-icon/components";
import Notifications from "../components/common/Notifications";
---

<Layout title="MarketFlex | Iniciar Sesión" simple={true}>
    <div class="login-container">
        <div class="login-card">
            <!-- Decorative Crosses Pattern -->
            <div
                class="decorative-cross decorative-dark"
                style="top: 15px; left: 15px;"
            >
            </div>
            <div
                class="decorative-cross decorative-dark"
                style="top: 15px; right: 15px;"
            >
            </div>
            <div
                class="decorative-cross decorative-dark"
                style="bottom: 15px; left: 15px;"
            >
            </div>
            <div
                class="decorative-cross decorative-dark"
                style="bottom: 15px; right: 15px;"
            >
            </div>

            <div
                class="decorative-cross decorative-x"
                style="top: 50%; left: 15px; opacity: 0.3"
            >
            </div>
            <div
                class="decorative-cross decorative-x"
                style="top: 50%; right: 15px; opacity: 0.3"
            >
            </div>

            <div
                class="decorative-cross decorative-darker"
                style="top: 15%; left: 50%; opacity: 0.2"
            >
            </div>
            <div
                class="decorative-cross decorative-darker"
                style="bottom: 15%; left: 50%; opacity: 0.2"
            >
            </div>

            <div
                class="decorative-cross decorative-x decorative-dark"
                style="top: 25%; left: 10%; opacity: 0.15"
            >
            </div>
            <div
                class="decorative-cross decorative-x decorative-dark"
                style="bottom: 25%; right: 10%; opacity: 0.15"
            >
            </div>

            <!-- Specific X shapes -->
            <div
                class="decorative-cross decorative-x decorative-darker"
                style="top: 10%; right: 40%; opacity: 0.15"
            >
            </div>
            <div
                class="decorative-cross decorative-x decorative-darker"
                style="bottom: 10%; left: 40%; opacity: 0.15"
            >
            </div>
            <div
                class="decorative-cross decorative-x"
                style="bottom: 40%; right: 5%; opacity: 0.1"
            >
            </div>
            <div
                class="decorative-cross decorative-x"
                style="top: 40%; left: 5%; opacity: 0.1"
            >
            </div>

            <div class="login-header">
                <a href="/" class="logo">
                    <img
                        src="/logo-marketflex-no-circle.png"
                        alt="MarketFlex Logo"
                        class="logo-img"
                    />
                </a>
                <h1>Bienvenido de nuevo</h1>
                <p>Ingresa a tu cuenta para continuar</p>
            </div>

            <form class="login-form">
                <div class="form-group">
                    <label for="email">Correo Electrónico</label>
                    <div class="input-wrapper">
                        <Icon name="lucide:mail" class="input-icon" />
                        <input
                            type="email"
                            id="email"
                            name="email"
                            placeholder="nombre@ejemplo.com"
                            required
                        />
                    </div>
                </div>

                <div class="form-group">
                    <label for="password">Contraseña</label>
                    <div class="input-wrapper">
                        <Icon name="lucide:lock" class="input-icon" />
                        <input
                            type="password"
                            id="password"
                            name="password"
                            placeholder="••••••••"
                            required
                        />
                    </div>
                </div>

                <div class="form-options">
                    <label class="checkbox-label">
                        <input type="checkbox" name="remember" />
                        <span>Recordarme</span>
                    </label>
                    <a href="#" class="forgot-password"
                        >¿Olvidaste tu contraseña?</a
                    >
                </div>

                <button type="submit" class="btn-login">
                    Iniciar Sesión
                    <Icon name="lucide:arrow-right" class="btn-icon" />
                </button>
            </form>

            <div class="login-footer">
                <p>¿No tienes una cuenta? <a href="/register">Regístrate</a></p>
            </div>
        </div>
    </div>
    <Notifications
        client:only="react"
        message="Credenciales incorrectas"
        type="error"
        delay={100}
        position="top-center"
        requiredQueryParam="error"
    />
</Layout>

<script>
    import { login } from "../services/auth.service";

    const loginForm = document.querySelector(".login-form") as HTMLFormElement;
    const emailInput = document.getElementById("email") as HTMLInputElement;
    const rememberCheckbox = document.querySelector(
        "input[name='remember']",
    ) as HTMLInputElement;

    // Load remembered email
    const rememberedEmail = localStorage.getItem("rememberedEmail");
    if (rememberedEmail) {
        emailInput.value = rememberedEmail;
        rememberCheckbox.checked = true;
    }

    loginForm?.addEventListener("submit", async (e) => {
        e.preventDefault();
        const formData = new FormData(loginForm);
        const data = Object.fromEntries(formData);
        const submitBtn = loginForm.querySelector(
            ".btn-login",
        ) as HTMLButtonElement;

        try {
            submitBtn.disabled = true;
            submitBtn.textContent = "Iniciando...";

            const response = await login({
                email: data.email,
                password: data.password,
            });

            // Handle Remember Me
            if (rememberCheckbox.checked) {
                localStorage.setItem("rememberedEmail", data.email as string);
            } else {
                localStorage.removeItem("rememberedEmail");
            }

            // Save session
            localStorage.setItem("token", response.token);
            localStorage.setItem("user", JSON.stringify(response.user));

            // Redirect
            // Redirect
            // Redirect
            // Use window.location.replace to prevent back button from returning to login
            const userName = response.user.nombre || "Usuario";
            window.location.href = `/?login_success=true&user=${encodeURIComponent(userName)}`;
        } catch (error) {
            console.error("Login error:", error);
            // Reload with error param to trigger notification
            window.location.href = "/login?error=true";

            submitBtn.disabled = false;
        }
    });
</script>

<style>
    .login-container {
        min-height: 80vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
    }

    .login-card {
        background: var(--foreground);
        backdrop-filter: blur(30px);
        border: 1px solid rgba(255, 255, 255, 0.05);
        border-top: 4px solid var(--green-primary);
        padding: 2.5rem;
        border-radius: 24px;
        width: 100%;
        max-width: 420px;
        box-shadow:
            0 20px 40px -10px rgba(0, 0, 0, 0.3),
            0 0 20px rgba(74, 222, 128, 0.1);
        position: relative;
        overflow: hidden;
    }

    .login-header {
        text-align: center;
        margin-bottom: 2rem;
        position: relative;
    }

    .login-header::after {
        content: "";
        display: block;
        width: 40px;
        height: 3px;
        background: var(--green-primary);
        margin: 1.5rem auto 0;
        border-radius: 2px;
    }

    /* Decorative Crosses */
    .decorative-cross {
        position: absolute;
        width: 12px;
        height: 12px;
        opacity: 0.4;
    }

    .decorative-cross::before,
    .decorative-cross::after {
        content: "";
        position: absolute;
        background: var(--green-primary);
        border-radius: 1px;
    }

    .decorative-cross::before {
        top: 5px;
        left: 0;
        width: 100%;
        height: 2px;
    }

    .decorative-cross::after {
        top: 0;
        left: 5px;
        height: 100%;
        width: 2px;
    }

    .decorative-cross.decorative-x {
        transform: rotate(45deg);
    }

    .decorative-cross.decorative-dark::before,
    .decorative-cross.decorative-dark::after {
        background: var(--green-accent);
    }

    .decorative-cross.decorative-darker::before,
    .decorative-cross.decorative-darker::after {
        background: var(--green-800);
    }

    .logo {
        display: inline-block;
        text-decoration: none;
        margin-bottom: 1.5rem;
    }

    .logo-img {
        height: 100px;
        width: auto;
    }

    .login-header h1 {
        font-size: 1.75rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
        color: #1a1a1a;
    }

    .login-header p {
        color: #666;
        font-size: 0.95rem;
    }

    .login-form {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
    }

    .form-group {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .form-group label {
        font-size: 0.9rem;
        font-weight: 500;
        color: #1a1a1a;
        margin-left: 0.25rem;
    }

    .input-wrapper {
        position: relative;
    }

    .input-icon {
        position: absolute;
        left: 1rem;
        top: 50%;
        transform: translateY(-50%);
        color: #666;
        width: 18px;
        height: 18px;
        pointer-events: none;
        transition: color 0.3s ease;
    }

    input {
        width: 100%;
        padding: 0.8rem 1rem 0.8rem 2.8rem;
        background: #f5f7fa;
        border: 1px solid #e1e4e8;
        border-radius: 12px;
        color: #1a1a1a;
        font-size: 1rem;
        transition: all 0.3s ease;
    }

    input:focus {
        outline: none;
        border-color: var(--green-primary);
        background: #ffffff;
        box-shadow: 0 0 0 4px rgba(74, 222, 128, 0.2);
    }

    input:focus + .input-icon {
        color: var(--green-primary);
    }

    .form-options {
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 0.9rem;
    }

    .checkbox-label {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        cursor: pointer;
        color: #666;
    }

    .forgot-password {
        color: var(--green-primary);
        text-decoration: none;
        font-weight: 500;
        transition: opacity 0.2s ease;
    }

    .forgot-password:hover {
        opacity: 0.8;
        text-decoration: underline;
    }

    .btn-login {
        background: var(--green-primary);
        color: white;
        border: none;
        padding: 1rem;
        border-radius: 12px;
        font-weight: 600;
        font-size: 1rem;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        transition: all 0.2s ease;
        margin-top: 0.5rem;
    }

    .btn-login:hover {
        background: var(--green-accent);
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(var(--green-primary), 0.3);
    }

    .login-footer {
        margin-top: 2rem;
        text-align: center;
        font-size: 0.9rem;
        color: #666;
    }

    .login-footer a {
        color: var(--green-primary);
        text-decoration: none;
        font-weight: 600;
        margin-left: 0.25rem;
    }

    .login-footer a:hover {
        text-decoration: underline;
    }
</style>
