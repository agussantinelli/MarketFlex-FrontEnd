---
import ManagementLayout from "../../layouts/ManagementLayout.astro";
import ProductsListView from "../../components/management/ProductsListView.tsx";
import ConfirmationModal from "../../components/common/ConfirmationModal.astro";
import modalStyles from "../../components/common/styles/ConfirmationModal.module.css";
---

<ManagementLayout title="Gestión de Productos">
    <ProductsListView client:only="react" />

    <ConfirmationModal
        id="delete-product-modal"
        title="Borrar Producto"
        message="¿Estás seguro de que deseas borrar este producto? Pasará a estado BORRADO y se ocultará del catálogo."
        confirmText="Borrar"
        cancelText="Cancelar"
        type="danger"
    />
</ManagementLayout>

<script is:inline define:vars={{ modalStyles }}>
    window.showDeleteProductModal = (onConfirm) => {
        const modal = document.getElementById("delete-product-modal");
        const confirmBtn = document.getElementById(
            "delete-product-modal-confirm",
        );
        const cancelBtn = document.getElementById(
            "delete-product-modal-cancel",
        );

        if (!modal || !confirmBtn || !cancelBtn) return;

        modal.classList.add(modalStyles.active);

        const handleConfirm = () => {
            onConfirm();
            modal.classList.remove(modalStyles.active);
            cleanup();
        };

        const handleCancel = () => {
            modal.classList.remove(modalStyles.active);
            cleanup();
        };

        const cleanup = () => {
            confirmBtn.removeEventListener("click", handleConfirm);
            cancelBtn.removeEventListener("click", handleCancel);
        };

        confirmBtn.addEventListener("click", handleConfirm);
        cancelBtn.addEventListener("click", handleCancel);
    };
</script>

