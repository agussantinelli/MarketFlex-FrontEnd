---
import Layout from '../layouts/Layout.astro';
---

<Layout title="MarketFlex | Premium Marketplace">
	<div class="hero">
		<h1 class="gradient-text">MarketFlex</h1>
		<p class="subtitle">Experience the future of commerce.</p>
		<div class="cta-group">
			<a href="#" class="btn btn-primary">Get Started</a>
			<a href="#" class="btn btn-secondary">Learn More</a>
		</div>
	</div>

	<div class="features">
		<div class="premium-card">
			<h3>Fast Performance</h3>
			<p>Built with Astro for lightning fast page loads and SEO optimization.</p>
		</div>
		<div class="premium-card">
			<h3>Modern UI</h3>
			<p>A sleek, premium design system focused on user experience.</p>
		</div>
		<div class="premium-card">
			<h3>Flexible Scale</h3>
			<p>Ready to handle your growth with a scalable architecture.</p>
		</div>
	</div>
</Layout>

<style>
	.hero {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		padding: 8rem 2rem;
		text-align: center;
		background: radial-gradient(circle at center, rgba(0, 112, 243, 0.1) 0%, transparent 70%);
	}

	h1 {
		font-size: 5rem;
		margin-bottom: 1rem;
		letter-spacing: -2px;
	}

	.subtitle {
		font-size: 1.5rem;
		color: rgba(255, 255, 255, 0.7);
		margin-bottom: 3rem;
		max-width: 600px;
	}

	.cta-group {
		display: flex;
		gap: 1rem;
	}

	.btn-secondary {
		border: 1px solid rgba(255, 255, 255, 0.2);
		color: white;
	}

	.btn-secondary:hover {
		background: rgba(255, 255, 255, 0.1);
	}

	.features {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
		padding: 4rem 2rem;
		max-width: 1200px;
		margin: 0 auto;
	}

	h3 {
		margin-bottom: 1rem;
		color: var(--primary);
	}

	p {
		color: rgba(255, 255, 255, 0.8);
	}

	@media (max-width: 768px) {
		h1 {
			font-size: 3rem;
		}
	}
</style>
